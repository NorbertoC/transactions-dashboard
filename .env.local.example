# ==============================================
# ENVIRONMENT VARIABLES TEMPLATE
# ==============================================
# Copy this file to .env.local and fill in your actual values
# ==============================================

# API Configuration
NEXT_PUBLIC_API_URL=https://api.example.com/transactions
API_KEY=your-api-key-here

# NextAuth.js Configuration (REQUIRED)
# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secret-jwt-key-here-minimum-32-characters-long
# Local: http://localhost:3000
# Production: https://your-domain.com
NEXTAUTH_URL=http://localhost:3000

# Google OAuth Configuration
# Get from: https://console.cloud.google.com/apis/credentials
# Authorized redirect URIs (add both in Google Console):
#   - http://localhost:3000/api/auth/callback/google
#   - https://your-domain.com/api/auth/callback/google
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Email Whitelist (Only these emails can login via Google)
ALLOWED_EMAIL_1=your-email@gmail.com
ALLOWED_EMAIL_2=spouse-email@gmail.com
# Add more as needed: ALLOWED_EMAIL_3, ALLOWED_EMAIL_4, etc.

# Optional: Username/Password Login (backup authentication)
USER_1_USERNAME=user1
USER_1_PASSWORD=change_this_password

USER_2_USERNAME=user2
USER_2_PASSWORD=change_this_password